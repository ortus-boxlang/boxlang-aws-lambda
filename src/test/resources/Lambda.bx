class{

	function run( event, context, response ){
		println( "====> Incoming event " & event.toString() );
	}

	function hello( event, context, response ){
		println( "====> Hello Baby " & event.toString() );
		return "Hello Baby";
	}
}
