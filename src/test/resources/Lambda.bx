class{

	function run( event, context, response ){
		println( "====> Incoming event " );
		println( event )
		println( response )
	}

	function hello( event, context, response ){
		println( "====> Hello Baby " );
		println( event )
		return "Hello Baby";
	}
}
