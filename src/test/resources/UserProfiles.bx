class{

	function run( event, context, response ){
		return {
			"message": "Test: UserProfiles handling hyphenated URI",
			"data": [
				{ "id": 1, "username": "user1", "profile": "active" },
				{ "id": 2, "username": "user2", "profile": "pending" }
			],
			"route": "user-profiles -> UserProfiles.bx"
		};
	}
}
